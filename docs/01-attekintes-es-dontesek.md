# 1. Áttekintés és döntések

## Mit vált ki

A Mándoki Dental jelenleg egy `.xls` munkafüzetben készíti a kezelési
terveket. A munkafüzet két lapból áll: `Kezelesi_Terv` (a nyomtatandó
dokumentum) és `Arlista` (157 sor).

**Nincs benne valódi makró.** A VBA projekt egyetlen üres
`Sub Lenyíló27_Változáskor()`-t tartalmaz. Amit a doktor makróknak hív,
az form control legördülők + `INDEX()` képletek kombinációja:

```
I14 = INDEX(Arlista!$B$1:$B$196, Arlista!$C1)   ; C1 = a combo box LinkedCell-je
K14 = I14 * J14
K23 = SUM(K14:K22)
I48 = K23 + K36 + K46
```

### Miért nem elég az Excelt megjavítani

A doktor négy kérése közül három megoldható lenne a táblában is
(egységár-oszlop felszabadítása, második ár-oszlop, nyomtatási terület
logóval). Az alkalmazás valódi indokai ezek:

1. **Az árlookup sorindex alapú.** Ha bárki beszúr egy sort az `Arlista`
   közepére, minden korábban mentett terv csendben más árat mutat. Ez néma
   adatkorrupció egy olyan dokumentumban, amit a páciens aláír.
2. **A tábla már most szétesett.** A LinkedCell-ek `C1..C5, C9, C11` —
   a saját kommentjei szerint *"A 4. sor nem működik, ezért kihagyva!"*.
3. **Kemény limitek:** maximum 3 kezelési fázis, fázisonként 7–9 sor.
4. **Nincs tervtörténet és nincs verziókövetés.**

## Döntések

| # | Döntés | Indoklás / következmény |
|---|---|---|
| D1 | Egy rendelő, belső eszköz — nem termék | Nincs multi-tenancy, nincs auth a fő flow-ban |
| D2 | Nincs szerveroldali páciensadat | A fejlesztő soha nem lesz adatfeldolgozó; nincs DB, nincs adatfeldolgozói szerződés |
| D3 | A fájlrendszer a system of record | A doki kijelöl egy gyökérmappát, az app oda ír. Google Drive-val szinkronizálja |
| D4 | Soha nem írunk felül tervet | Módosításkor mindig új verziómappa (`v2`). Aláírt szerződést nem lehet visszamenőleg átírni |
| D5 | A PDF és a `terv.json` együtt íródik | Plusz a JSON **beágyazva a PDF-be** is — így a PDF önhordozó marad, ha külön elküldik |
| D6 | Ártétel-hivatkozás stabil `id`-vel, nem sorindexszel | Ez az eredeti Excel fő hibája |
| D7 | Az ajánlat pillanatkép | A terv sorai snapshotolják a tételnevet és az árat; az árlista későbbi változása nem írja felül a múltat |
| D8 | Kedvezmény = `listaEgysegar` és `tenylegesEgysegar` külön tárolva | A kedvezmény származtatott, tehát mérhető. Nem felülírás |
| D9 | A kedvezmény **nem jelenik meg** a nyomtatványon | Nem volt rá igény; a doki szóban kommunikálja |
| D10 | Kétnyelvű modell az első naptól, magyar tartalommal indul | A német nyelv egy kapcsoló, ami a fordítások elkészülte előtt is bekapcsolható — a hiány az appban számszerűen látszik (lásd D21) |
| D11 | HUF és EUR ár egymástól függetlenül szerkeszthető | Nincs árfolyam-átváltás, nincs külső hívás |
| D12 | Tetszőleges számú fázis és sor | Az Excel 3×7 limitje tisztán technikai artefaktum volt |
| D13 | Egy `Fog` mező soronként, szabad felsorolás | Nem külön megjegyzés oszlop. Megjegyzés fázis szinten van |
| D14 | Egységtípus (fogankénti/alkalmankénti) explicit besorolása **kimarad** — az automatikus darabszám azóta heurisztikával megvalósult (lásd D32) | A 118 tétel egységtípus-besorolása hetekre elakasztotta volna a projektet; a heurisztika (`parseTeeth()` mindent-vagy-semmit logikája) ugyanezt a célt éri el explicit adminmunka nélkül |
| D15 | Sávos árú tétel jelölése `*` + lábjegyzet | Jogi védelem: a sávos ár fix számként nyomtatva kötelező érvényű ajánlattá válna. A `*` forrása a **sor** `savos` mezője, nem az árlistai ártípus — a doki soronként kézzel is átbillentheti egy fix árú tételt becsültre, ha a mennyiség csak a kezelés során derül ki |
| D16 | Minden nem-null Excel sor importálódik | A takarítás az adminban történik, nem az importban |
| D17 | Tétel inaktiválható, de nem törölhető | Az `id` soha nem használható újra — a régi tervek értelmezhetősége múlik rajta |
| D18 | Minden JSON fájl `schemaVersion` mezővel indul | Ezek a fájlok évekig élnek a Drive-on; a 3. verziónak is olvasnia kell a mait |
| D19 | Search-only tételkereső, nincs kategória böngésző | A doki fejből tudja a tételeket; a keresés ékezetfüggetlen |
| D20 | „Gyakori" tételek kézzel jelölve | Nem használati statisztikából — kiszámítható, nem ugrál a UI |
| D21 | A terv nyelve és pénzneme egymástól **függetlenül** választható; a pénzösszeg ezres/tizedes elválasztója a **nyelvtől**, a tizedesjegyek száma és a pénznemjel a **pénznemtől** függ (52. tétel, D63) | A német páciens Magyarországon forintban is fizethet. Az 1:1 kötés (`de` → EUR) rossz tervezés lenne még kész fordítás/árazás mellett is — a nyelv a nyomtatvány szövegét vezérli (tételnevek, feliratok, dátumformátum, sablon, pénzformátum), a pénznem az ajánlható tételkört és a tizedesjegyeket/pénznemjelet. Emellett a döntés idején (és amíg a doki nem lektorálta a 118 gépi fordítást és becsült EUR árat, lásd `docs/06-arlista-import.md`) a kötés használhatatlanná is tenné a német módot |
| D22 | Korábbi terv új verzióra nyitásakor a `keltezes` és az `ervenyesIg` a betöltés pillanatában frissül a mai napra (és az aktuális `ervenyessegNap`-ra), nem véglegesítéskor | Visszatöltött terv különben hónapokkal korábbi, akár lejárt keltezéssel nyomtatna. A véglegesítéskori írás a mentett JSON-t és a már renderelt PDF-blobot szétcsúsztatná. Csak dokumentum-metaadat változik: a sorok ára és `nevSnapshot`-ja pillanatkép marad (D7) |
| D23 | Placeholder-jelölésű (`[PLACEHOLDER`/`[PLATZHALTER`) nyilatkozat esetén a nyomtatvány nyilatkozat és aláírás oldala nem nyomtatható — a „csak ajánlat" mód kényszerített és letiltott, felülírás nélkül | Jogi kockázat: a páciens elé nem kerülhet aláírandó lap, amit a jogász „még nincs lezárva" jelöléssel látott el. A fizetési feltételek és a garancia `offerOnly` módban is nyomtatódik, ezért azoknál a magyar szövegre visszaesés + jelzés a helyes válasz, nem zár |
| D24 | Kézzel megadott sornevet automatikus mechanizmus soha nem ír felül némán — nyelvváltáskor csak az a `tetelId`-hez kötött sor frissül, ami a váltás előtti nyelven még pontosan az árlistai nevet viselte | A `nevSnapshot` szerkeszthető; a feltétel nélküli újraírás törölné a doki pontosítását. Új sémamező nélkül eldönthető (`nevSnapshot === tetel.nev[nyelv]`); az eltérést az „átírt" jelvény és a véglegesítés megerősítő listája teszi láthatóvá |
| D25 | A terv-szintű „kerek végösszeg" kedvezmény (`Plan.kedvezmenyOsszeg`) FIX összegként tárolódik, nem a doki által begépelt cél-végösszegként | Ha a cél-végösszeget tárolnánk élőben, egy utólagos sormódosítás némán, a doki tudta nélkül változtatná meg a kedvezményt — ez D8 szellemével (a kedvezmény mérhető, explicit tényállapot) ütközne. A `Fizetendő` emellett soha nem megy 0 alá: ha a fix kedvezmény egy utólagos sortörlés után meghaladja a sorok összegét, a végösszeg 0-ra padlózódik, nem negatív szám kerül az aláírandó papírra (`tervVegosszeg()`, `domain/totals.ts`) |
| D26 | Egy terv másolása (akár csak a páciensadattal, akár a sorokkal együtt) mindig új tervláncot indít — üres `tervId`, `verzio: 0`, `PISZKOZAT` státusz, mai keltezés — és a másolat `osszesitok`-ja a saját (átvett) soraiból újraszámolva indul, nem a forrás mentett értékének másolata. A `paciensId` (D29) átöröklődik, tehát az új lánc a MEGLÉVŐ páciens-mappában nyílik, nem egy újban | D4 kiegészítése: a másolat sosem csúszhat be verzióként egy meglévő, esetleg aláírt láncba. A forrás `osszesitok`-ja az EREDETI, már mentett terv fájl-igazsága (D7) — egy soha nem mentett piszkozatra másolva hamis „mentett vs. újraszámolt” eltérést jelezne |
| D27 | A `Tetel.leiras` hiányzó német fordítása némán elmarad a nyomtatványról, nem esik vissza magyarra (ellentétben a `nevSnapshot`-tal) | A leírás kiegészítő, díszítő tartalom, nem a sor lényege (azt a név hordozza) — egy vegyes nyelvű nyomtatvány rosszabb lenne, mint a leírás hiánya. A `nevSnapshot`-hoz hasonló szigorú HU-visszaesés/jelvény-apparátus túlkezelés lenne egy opcionális mezőhöz (docs/02-domain-modell.md § Tétel-leírás) |
| D28 | A fogtérkép kezelés-kategóriánkénti színe az árlista `Kategoria.szin` mezőjéből olvas, nem kódba huzalozott táblából; egy fogon több kezelés esetén a kategórialista sorrendje egyben az ütközési prioritás (a legkisebb `sorrend`-ű kategória színe nyer) | A szín így a doki kezében marad a Kategóriák panelen, kódmódosítás nélkül; a prioritás magyarázat nélkül is kiszámítható, mert ugyanaz a sorrend, amit ő állított be (`docs/07-felulet-rendszer.md` § Szín, forma, sűrűség, `resolveToothVisual`) |
| D29 | A páciens explicit, azonosított entitás (`paciensId`, `paciens.json`) egy köztes szinten a páciens-mappa és a terv-mappák között — egy páciens-mappa TÖBB terv-láncot (terv-mappát) is tartalmazhat, nem csak egyet. A `paciens.json`/`terv-cimke.json` kizárólag azonosító-/kereső-index és szervezési metaadat, SOHA nem system of record: a `terv.json` `paciens` blokkja marad a pillanatkép (D7), a terv-mappa neve a létrehozáskor fix, a megjelenített terv-címke attól függetlenül szabadon szerkeszthető | A korábbi névismétlés-alapú felismerés (két Kovács János is lehet, egy név a doki keze alatt is változhat) tévesen összevonna/szétválasztana pácienseket; egy explicit azonosító ezt kizárja. A `paciens.json` csak index (D3: a fájlrendszer a system of record, de a keresést/előtöltést nem kell minden alkalommal a teljes fa bejárásával megoldani) |
| D30 | Az `arlista.json` `arlistaVerzio` mezője az Árlista admin MINDEN mentésekor (`commit()`) a mai napra áll, mezőnkénti különbségtevés nélkül — akkor is, ha a szerkesztés csak egy `gyakori`-csillagozás | A kockázat aszimmetrikus: a régi (frissítés nélküli) állapot **alul-jelzett** — a nyomtatvány lábléce örökre a seed-dátumot mondta volna, hamis audit-adatként. A feltétel nélküli bump legrosszabb esete **túl-jelzés** (egy tartalmilag semleges szerkesztés is bump-ol), ami ártalmatlan. Egy mezőnkénti finomítás mélyebb régi/új-összehasonlítást igényelne egy olyan pontosságért, aminek hiánya nem okoz kárt. A már mentett terveken lévő `arlistaVerzio` ettől függetlenül pillanatkép marad (D7) |
| D31 | A `PlanStorage`-t fogyasztó `savePriceList`/`saveSettings` (`AppState.tsx`) KIZÁRÓLAG updatert fogad (`(prev) => next`), sosem kész objektumot; a memóriabeli állapot a mentés ELŐTT, szinkron frissül (optimista), és sikertelen mentésre NEM gördül vissza. Ha az implementáció írása nem atomi, neki kell sorosítania az egymást követő írásokat (lásd `DemoStorage` `enqueue`/`savingChain`) | Egy render-idejű closure-be zárt régi állapotra épülő mentés (a korábbi `{ ...priceList, ... }`/`{ ...settings, ... }` minta) két, gyorsan egymást követő szerkesztésnél némán eldobja az egyiket — ez a doki törzsadatát (árlista, rendelő-adat) rontja el, nem egy tervet. Ma (`localStorage`, szinkron írás) a versenyablak kicsi, de a `FileSystemStorage`-váltás (`docs/05-technologia.md`, 2. fázis) alatt az írási késleltetés nagyságrendekkel nő, és ugyanez a hiba néma adatvesztéssé válna. A visszagördülés hiánya szándékos: konkurens írásoknál egy korábbi állapotra való visszaállás egy közben sikeresen mentett MÁSIK szerkesztést is eldobna |
| D32 | A sor `mennyiseg` (darabszám) mezője automatikusan követi a `fogak` mezőt, amíg a doki kézzel felül nem írja a darabszámot (`Sor.mennyisegKezi`) — attól kezdve a sor levált, a fogak további módosítása többé nem írja felül némán. Hiányzó `mennyisegKezi` (egy, a funkció bevezetése előtti sor) kézinek számít, nem automatikusan követőnek | D14 részleges újranyitása: a fogak-alapú heurisztika (`parseTeeth()`) explicit egységtípus-besorolás nélkül old meg egy valódi UX-hiányt. A D24 mintáját követi (kézzel megadott érték automatikus mechanizmus által soha nem íródik felül némán) — enélkül egy régi, szándékosan eltérő darabszámú terv csendben átíródna (`docs/02-domain-modell.md` § Fogszám kezelés) |
| D33 | Új, önálló fájl (`paciens-adatok.json`, a páciens-mappa gyökerén) a páciens ÉLŐ, terv-mentéstől független törzsadatának — VALÓDI system of record a saját mezőire, ellentétben a `paciens.json`/`terv-cimke.json` puszta index-fájljaival (D29). Nincs automatikus szinkron a `terv.json` `paciens` blokkjával egyik irányban sem; amíg nem létezik, élő fallback mutatja a páciens legutóbb módosított terv-láncának legfrissebb `paciens` pillanatképét, első mentéskor a teljes fájl egyszerre zár | A `terv.json` `paciens` blokkja tervenkénti pillanatkép (D7) — nincs hely, ahol a doki a páciens jelenleg érvényes elérhetőségét tartaná, terv-mentéstől függetlenül. NEM D29 pontosítása: a `paciens.json` szerepe és a D29 szövege változatlan marad, ez egy párhuzamos, önálló adatforrás (`docs/02-domain-modell.md` § Páciens-szintű törzsadat) |
| D34 | Végleges, öt tételes fő navigáció: `Kezdőlap | Páciensek | Kezelések és árak | Beállítások | DEMO`. A demó-only felületek (a fájlrendszer-vizualizáció, a változásnapló, a funkciólista) a `DEMO` menüpont alá tartoznak, nem az üzleti navigációba — az `Árlista admin` felhasználói neve `Kezelések és árak` lesz, a modul/route belső neve változatlan | A doki-validáció golden pathje (páciens → terv → kezelés → PDF) ne keveredjen a fejlesztési/demonstrációs felülettel (fájlfa, changelog) egy szinten; ez utóbbiak a `FileSystemStorage`-váltás (2. fázis) után amúgy sem élnek tovább. A `Páciens`/`Terv szerkesztő`/`Előnézet`/`Korábbi tervek` linkek átmenetileg a navon maradtak, amíg a terv-workflow héj (D36) át nem vette a szerepüket |
| D35 | URL-lel címezhető páciens-részletoldal (`/paciensek/:patientDir`, két tabbal: `Páciens adatai` \| `Kezelési tervek`) — a Páciensek és a Korábbi tervek listái közti kereszt-linkek innentől erre az URL-re navigálnak, a kezdő tabot `location.state`-ben jelezve, nem a régi két külön top-level oldalra tranziens `location.state.patientDir`-rel | A korábbi kereszt-linkek `location.state`-tel adták át, melyik pácienst kell megnyitni — ez frissítésnél (F5) elveszett, és mindkét listaoldalon egy külön, egymással duplikált "görgess a sorra és nyisd ki" boilerplate-et igényelt. Az URL-ben hordozott azonosító megoldja mindkettőt: könyvjelezhető/frissítés-biztos, és a görgetés-effektek megszűnnek |
| D36 | A `/paciens`/`/terv`/`/elonezet` workflow-oldalakat közös héj veszi körül: állandó, kattintható breadcrumb (`Páciensek > [páciens neve]`, a páciens-szegmens D37 szerint feltételesen link) + szabadon kattintható, route-vezérelt 3-lépéses stepper (`Terv adatai → Kezelések → Előnézet és véglegesítés`). A stepper a meglévő, laponkénti "Tovább" gombok MELLETT él, nem helyettük; ezzel a fő navigáció véglegesen ötelemű, a D34-ben átmenetileg megtartott négy workflow-link (`Páciens`/`Terv szerkesztő`/`Előnézet`/`Korábbi tervek`) megszűnt | A stepper formalizálja azt, ami korábban is igaz volt (a workflow-oldalak között szabadon lehetett navigálni) — a lépésjelző és az irányított "Tovább" CTA együtt élnek, nem egymást kizáró minta |
| D37 | A piszkozat perzisztált rekordja (`DraftRecord`, `app/src/storage/DraftStorage.ts`) két opcionális UI-workflow metaadatot hordoz a `Plan` MELLETT (nem azon): `patientDir` (a draft best-effort ismert páciens-mappája) és `lastRoute` (az utoljára meglátogatott workflow-route). Egyik sem a terv tartalma — nem `Plan`-mező, nem kerül papírra. A `patientDir`-t csak azok a helyek töltik ki, ahol a hívó már ismeri (pl. "Korábbi tervek" akciói, "Meglévő páciens keresése", a D41 óta a "+ Új páciens" quick-create sikeres mentése is) — hiánya csak egy funkció előtti perzisztált piszkozatnál marad. A `lastRoute`-ot a terv-workflow héj (D36) írja, route-váltáskor | A Kezdőlap "Piszkozat folytatása" gombja korábban egy névkitöltés-heurisztikával találgatta, hova navigáljon (sosem az Előnézetre); a breadcrumb páciens-szegmense (D36) emiatt maradt link nélkül. Mindkettő best-effort: hiányuk nem hibaállapot, csak a régi fallback-viselkedés (a heurisztika, illetve a sima szöveg) lép életbe |
| D38 | Egy explicit Mentés/Mégse gombpárral szerkesztett mezőkészletnél (nem autosave-nél, lásd D31) a mentési hiba SOHA nem írja vissza és nem dobja el a szerkesztőben lévő piszkozatot, és a szerkesztett elem elhagyása (sor-/tab-váltás, Mégse) mentetlen módosításnál megerősítést kér — egy közös hook (`useDirtyDraft`) és egy közös dialógus-komponens (`DiscardChangesDialog`) mögött. Nincs böngésző-/router-szintű navigáció-blokkolás (a `HashRouter` ezt eleve nem is támogatná data router nélkül) — a guard hatóköre eredetileg kizárólag a lapon belüli elem-váltás volt; a NavBar-navigációra azóta kiterjed, lásd D46 | Korábban három, egymástól független dirty-detektálás és ötszörösen másolt megerősítő dialógus élt egymás mellett; a `PatientEditorPanel` MA IS bevált `JSON.stringify`-alapú mély-egyenlőségét emeli ki egy tetszőleges (nem feltétlenül immutable-update mintájú) mezőkészletre is biztonságosan működő közös primitívvé |
| D39 | A Kezdőlap minimalista: fő CTA (`+ Új kezelési terv`) + aktív-draft blokk (D37, változatlan) + max 5 „legutóbbi páciens", a páciens `paciens.json`-jának új, opcionális `utolsoAktivitas` mezője (`{ tipus, idopont }`) szerint csökkenő sorrendben. A mező KIZÁRÓLAG három tényleges írási eseményen frissül (páciens létrehozása, törzsadat mentése, terv véglegesítése) — egy páciens/terv puszta megnyitása sosem ír bele. A demó-only „Demó adat visszaállítása"/„Minden adat törlése" a DEMO oldal új, negyedik (Adatkezelés) fülére költözött; a „Korábbi tervek" gomb megszűnt, a globális `/tervek` lista D34 szerint mostantól kizárólag URL-ről érhető el | A korábbi Kezdőlap öt egymásra épülő kártyát/gombot mutatott egyszerre, demó-eszközök és üzleti navigáció keverten, és sehol nem volt látható, kikkel dolgozott a doki nemrég — egy páciensenkénti wall-clock időbélyeg addig sehol nem létezett a rendszerben (a `PlanVersion.isoDate`/`Plan.keltezes` a doki által szabadon szerkeszthető ÜZLETI dátum, D22, nem mentési időpont). Puszta index-mezőként (D29) egy sérült/ismeretlen `utolsoAktivitas` érték némán kimarad a listából, nem hibát dob |
| D40 | Az „Új terv indítása" köztes páciensválasztó (D29) kereső mezője kétállású: 0–1 karakternél a recent-lista (max 15 — D56, a Kezdőlap D39 5-ös limitjétől eltérően, ugyanazzal a helperrel, más `limit`-paraméterrel), 2+ karaktertől relevancia szerinti rendezés (teljes név eleje > szótöredék eleje > belső egyezés, azon belül alfabetikus), a tételkeresővel (`ItemPicker.tsx`) azonos gépel → nyíl → Enter/Esc billentyűzet-ciklussal. Nulla találatnál egy közvetlen „Új páciens: „…"" opció a begépelt névvel, a mindig látható, a kereső fölött álló „+ Új páciens" gomb mellett (a gomb rangja/helye D55 szerint azóta elsődleges) | Ez a képernyő minden új tervnél megnyílik, mégis autofókusz és billentyűzet-navigáció nélkül, mindig teljes/alfabetikus listával indult — pont az a ciklus hiányzott róla, amit a CLAUDE.md „A UX kritikus pontja" a tételkeresőnél kötelezővé tesz |
| D41 | Az „Új terv indítása" köztes páciensválasztó (D29/D40) „+ Új páciens" ága (D55, korábban „Vadonatúj páciens") (és a kereső nulla találatos „Új páciens: „…"" opciója) a `PaciensekPage.tsx` „+ Új páciens"-ével közös quick-create dialógust nyitja meg — csak sikeres mentés után navigál tovább a Terv adatai lapra. A dialógus születési dátumot/telefont is felvehet (opcionális), és névegyezésnél cselekvő figyelmeztetést ad („Ezt a pácienst választom") | A terv-előtti valódi páciensrekord (D29) korábban csak a `/paciensek` belépési ponton teljesült; a „vadonatúj" ág üres `paciensId`-vel indult, a mappa csak a terv mentésekor materializálódott. Elfogadott mellékhatás: egy elhagyott piszkozat után is ottmarad a létrehozott páciensrekord — ez a Kezdőlap „Legutóbbi páciensek" (D39) listáján is megjelenik |
| D42 | A páciens-duplikáció felismerése kétfázisú: egy olcsó, csak a `listPatients()` eredményéből dolgozó névhasonlóság-szűrés (pontos vagy token-alapú hasonló egyezés), majd csak a szűk jelölt-körre betöltött születési dátum/telefon szűkíti tovább. Pontos névegyezés az ellentmondó születési dátum/telefon mellett is látszik (jelöléssel, külön megerősítéssel a kiválasztásnál); hasonló névegyezés ellentmondó adatnál kiesik. Nem blokkoló — a doki „Mégis új páciens létrehozása" explicit megerősítéssel felülbírálhatja. A quick-create dialóguson (`UjPaciensDialog.tsx`) kívül a páciens-törzsadat átnevezésekor (`PatientEditorPanel.tsx` Mentés gombja) is lefut, csak save-time, javaslat-lista nélkül | A `listPatients()` DOB/telefont nem ad (D29 — a `paciens.json` tisztán index), tehát ezeket minden begépelt karakterre minden páciensre betölteni drága lenne; a kétfázisúság ezt oldja fel anélkül, hogy a `paciens.json`-t denormalizált személyes adattal terhelné. A nem blokkoló jelleg szándékos: két azonos nevű, de valóban különböző páciens létezik (pl. apa-fia), a rendszer csak figyelmeztet, nem tilt |
| D43 | A Pácienslista (`PaciensekPage.tsx`) tiszta navigációs lista: a sorok a páciens-részletoldalra (D35) navigálnak, a törzsadat-szerkesztő (`PatientEditorPanel`) EGYETLEN helye onnantól a részletoldal `Páciens adatai` tabja. A sor kompakt (név + születési dátum + telefon), a két NORMÁL törzsadat-állapot (lezárt vs. élő fallback) nem kap jelvényt, kizárólag a betöltési hiba. A keresés névre (mint korábban) ÉS — 2+ begépelt számjegytől — a születési dátumra/telefonszámra is illeszkedik, elválasztójeltől függetlenül. A listáról egy sorra navigálva, majd böngésző-„vissza”-val visszatérve a keresőszöveg és a görgetési pozíció megmarad — kizárólag ezen az úton, egy friss (pl. NavBar-ról érkező) belépés mindig tiszta listát ad; a megőrzés a munkamenetre szűkített (nem böngészőtár), lapfrissítés után nem marad meg | A lista és a részletoldal (D35) korábban két, egymástól független belépési pontot tartott életben ugyanahhoz a szerkesztőhöz (inline accordion vs. tab) — ez a duplikáció szűnt meg. A kompakt sor a doki tényleges azonosítási igényét szolgálja (két azonos nevű páciens megkülönböztetése listanézetben, kinyitás nélkül); a két normál állapot jelvénye nem hordozott döntési információt, csak zajt, ezért esett ki. A state-megőrzés böngészőtár nélkül, csak POP-navigációra szól, hogy egy oda-vissza lapozgatás ne veszítse el a keresést, de egy hosszabb távú, böngészőtárba írt páciensnév-töredék se maradjon a gépen |
| D44 | A két hívóhelyű `PatientPlanChains` (`components/PatientPlanChains.tsx`) fejléc-elemei — a páciensnév és a `Páciens adatai` kereszt-link — a hívó felület adja meg, kötelező `header: 'standalone' \| 'embedded'` propon, alapértelmezés nélkül. `standalone` (Korábbi tervek listája, § 5): páciensnév + „Páciens adatai” kereszt-link + „Új terv” — itt ez az EGYETLEN páciens-azonosító és az EGYETLEN út a törzsadathoz. `embedded` (a páciens saját részletoldala, D35, `Kezelési tervek` tab): CSAK az „Új terv” (teljes értékű CTA-ként) és hiba esetén a ⚠ jelzés — a nevet a sticky fejléc, a törzsadat felé vezető utat a `Páciens adatai` tab-trigger már hordozza. Ugyanezért a `PatientEditorPanel` alján sincs „Korábbi tervek” tükör-link: a tabok közti váltás egyetlen helye a tabsor | Ugyanaz az azonosító/navigáció egy képernyőn kétszer nem redundancia-biztonság — a második, halványabb példány (ghost gomb a tab mellett) azt sugallja, hogy MÁS célra visz. A duplikáció azért maradt észrevétlen, mert a fejléc a Korábbi tervek több-pácienses listájáról KÖLTÖZÖTT a részletoldalra (D35) — ahol viszont valóban az egyetlen azonosító és az egyetlen útvonal — anélkül, hogy újratervezték volna. Az alapértelmezés hiánya szándékos: melyik alak a helyes, kizárólag a körülvevő felületből következik, amit a komponens nem ismerhet (a D33-nál a `PatientEditorPanel` callback-propjainál már alkalmazott elv) |
| D45 | A törzsadat-szerkesztő (`PatientEditorPanel`) kétállású: alapból READ-ONLY nézet (`ReadOnlyField`-ekkel), egy „Szerkesztés” gombbal az input-mezős nézetre váltva; a kitöltetlen mezők az app meglévő „—” hiányzó-érték jelölését kapják. Az e-mail-formátum és a jövőbeli születési dátum **blokkoló** mezővalidáció (a Mentés gomb nem tiltott, de a hibás mezőnél a mentés megszakad) — ugyanez a szabály a quick-create (`UjPaciensDialog.tsx`) Született mezőjére is vonatkozik. A név üressége változatlanul puha figyelmeztetés marad, nem blokkol | A puszta megnyitás ne indítson piszkozatot (véletlen módosítás kockázata); az üres-érték jelölésnél a meglévő „—” konvenció (`PatientPlanChains.tsx`, `pdf/TervDocument.tsx`) marad az egyetlen, hogy ne éljen két versengő „nincs adat” nyelv egymás mellett. Az e-mail/dátum blokkolás azért kell, mert a `paciens-adatok.json` valódi system of record (D33) — egy jövőbeli dátum vagy elgépelt e-mail némán, ellenőrizetlenül kerülne bele |
| D46 | A D38 „van nem mentett módosítás” védelme a NavBar öt linkjének kattintására is kiterjed — a `NavBar.tsx` a MEGLÉVŐ `useDiscardGuard`/`DiscardChangesDialog` primitívet hívja újra, egy megosztott, app-szintű „van piszkozat” jelzőn (`components/NavGuardContext.tsx` `NavGuardProvider`/`useNavGuard`) keresztül; a védett felület (`PatientDetailPage.tsx`, `SettingsPage.tsx`) a MÁR meglévő dirty state-jét regisztrálja egy plusz `useNavGuard(dirty)` hívással, explicit módon — nem automatikus minden `useDirtyDraft`-használatnál. Egyetlen boolean, nem kulcsolt regisztry: a HashRouter egyszerre egy route-ot renderel, tehát egyszerre kizárólag egy D38-védett felület lehet mountolva. Böngésző vissza/előre gomb, F5, URL-sáv átírás változatlanul védtelen marad — azokhoz valódi data router kellene, amit a `HashRouter` nem támogat | A doki a 39. tétel (D45) kézi tesztelése közben talált egy valós adatvesztést: NavBar-kattintással a piszkozat figyelmeztetés nélkül elveszett, holott a lapon belüli tab-váltás már védett volt. A NavBar linkjei a SAJÁT `NavLink` komponenseink, a kattintásukat `onClick`+`preventDefault`-tal el lehet fogni — ehhez, a D38 eredeti feltételezésével ellentétben, NEM kell router-szintű blokkolás |
| D47 | A Pácienslista (`PaciensekPage.tsx`) sora oszlopos táblázat (Radix `Table`, fejléccel: Név / Született / Telefon), a Kezdőlap „Legutóbbi páciensek” (D39) sorától (`components/PatientListRow.tsx`) SZÁNDÉKOSAN külön komponens (`pages/paciensek/PatientTableRow.tsx`) — ez D43 „azonos komponens” állítását ide vonatkozóan felülírja. Hiányzó születési dátum/telefon az app „—” jelölését kapja (nem üres cella); a törzsadat-betöltési hiba egy összevont, két oszlopot átfogó cellában jelenik meg | A két lista elrendezése nem közös: az oszlopos táblázat letapogatható rácsot ad, a Kezdőlap kompakt sora viszont az aktivitás-szöveget (D39) hordozza egy második sorként a név alatt — ez a `children`-rétegű elrendezés nem ültethető át egy táblázat-cellába torzítás nélkül. A hiányzó-érték „—” jelölés hiánya a korábbi, futó-szöveges sorban nem tűnt fel (a mező egyszerűen kimaradt); egy oszlopos rácsban egy néma üres cella olvashatatlan lenne a szomszéd sorokhoz képest |
| D48 | A páciens törzsadata (`paciens-adatok.json`, D33) és egy terv `paciens` pillanatképe közötti eltérés két külön, explicit irányú, mezőszintű művelettel kezelhető a Terv adatai lapon (`components/TorzsadatDiffDialog.tsx`) — soha nem egy közös „Szinkronizálás” gomb. A mezőszintű összevetésben alapból semmi nincs kijelölve. A „Terv adatai” lépés ELŐRE (Kezelések/Előnézet felé) elhagyásakor, ha VALÓDI ütközés áll fenn (mindkét oldalon van érték, és eltérnek — egy üres mező puszta pótlása nem számít ütközésnek), egyszer felkínálja a törzsadat-frissítést; ugyanarra a diff-re a workflow-n belül nem jelenik meg újra. Ha a páciensnek nincs törzsadata, egy információs blokk (nem hiba) jelzi, és a lépés elhagyásakor felkínálja annak azonnali létrehozását a terv adataiból. Master-írási hiba esetén a doki a helyszínen marad, Retry vagy Continue (írás nélküli továbblépés) választással. Véglegesítéskor a rendszer újraolvassa a mastert, és az eltérést egy INFO-szintű, nem blokkoló sorként mutatja — a véglegesítés emiatt sosem kér megerősítést | A D33 már kimondta, hogy nincs automatikus szinkron, de semmilyen felület nem mutatta meg vagy tette kezelhetővé az eltérést. A „csak valódi ütközésnél szakítja félbe” szabály nélkül a leggyakoribb eset (egy vadonatúj páciens a quick-create után kitölti a többi mezőt a Terv adatai lapon, miközben a törzsadat még csak a nevet tartalmazza) minden alkalommal felesleges megerősítést kérne — a puszta mezőpótlás nem adatvesztés-kockázat, csak az ütköző (mindkét oldalon eltérő értékű) mező az |
| D49 | A Beállítások három tabja (Radix `Tabs`, CONTROLLED): `Rendelő adatai` (alapértelmezett, a rendelő-mezők + Orvosok) \| `Nyomtatványok` (a sablonszövegek) \| `Egyéb` (ajánlat érvényessége, német nyelv, alapértelmezett nyelv, német készültség). Mindhárom tab saját, pufferelt draftot vezet explicit Mentés/Mégse gombpárral (`useDirtyDraft`) — a korábbi leütésenkénti autosave (D31) ezen a lapon megszűnt, az Árlista admin autosave-je változatlan. A Rendelő adatai/Egyéb Mégse azonnali; a Nyomtatványok Mégse (D38) megerősítést kér, mert egyszerre minden nyelv/szlot piszkozatát elveti. A tab-váltás (és a NavBar-navigáció, D46) nem mentett módosítással szintén megerősítést kér, és megerősítés után a piszkozat ténylegesen elvész — a Radix a tab tartalmát unmountolja, a Nyomtatványok `dp:sablon-piszkozat` localStorage-cache-e emiatt explicit törlődik, különben egy F5 a tab-váltás UTÁN visszahozná a már elvetett szöveget. A korábbi, nem konfigurálható „Logó” szekció (a `settings.logoFajl` mező sehol nem volt ténylegesen felhasználva — a PDF és a NavBar egy statikus `assets/logo.png` importot használ) törölve, a mezővel együtt | A lap korábban öt egymás alatti kártya volt, két, egymásnak ellentmondó mentési modellel: a legtöbb mező leütésenként mentett, az egyetlen alsó „Mentés” gomb viszont ténylegesen csak az Orvosok mezőt commitolta, ami `onBlur`-ra amúgy is megtörtént — a gomb gyakorlatilag no-op volt, miközben azt sugallta, addig semmi sincs mentve. A hármas felosztás szándékosan eltér a `backlog/redesign/`-ben felvetett `Rendelő \| Orvosok \| Dokumentum \| Tárolás` javaslattól: az Orvosok ma egy 3 soros textarea, nem érdemel önálló tabot; a `Tárolás` a 2. fázis (FileSystemStorage) tartalma, ma üres lenne |
| D50 | Páciens törölhető, kizárólag a páciens-részletoldal (D35) sticky fejlécének `⋯` menüjéből (`components/PatientDetailHeader.tsx` `actions` propja), és KIZÁRÓLAG ha nincs véglegesített (`statusz === 'VEGLEGES'`) terve, nincs rá mutató aktív, mentetlen piszkozata, és minden terv-lánca/verziója olvasható volt (`domain/paciensTorles.ts` `paciensTorlesAkadaly()`). Egyébként a menüpont tiltott, alatta rövid indoklással. A törlés (`PlanStorage.deletePatient(patientDir)`, az interfész első destruktív metódusa) a teljes páciensmappát elviszi, megerősítés után, véglegesen — nincs „kuka”, nincs helyreállítás, nincs páciens-összevonás | A quick-create (D41) és a duplikáció-detektálás (D42) óta egyre könnyebb véletlenül felvinni egy pácienst, de korábban nem volt visszaút. A törölhetőségi feltétel a D4 (verziómappa soha felül nem írható) adatintegritási korlátjának természetes kiterjesztése törlésre: egy aláírt/kiadott dokumentum vagy egy folyamatban lévő szerkesztés mögül nem törölhető a páciens. A draft-hovatartozást a MEGLÉVŐ `feloldPatientDir()` (D48) dönti el, nem új heurisztika |
| D51 | A terv-lánc/verzió fa (`components/PatientPlanChains.tsx`, mindkét hívón: `standalone`/`embedded`) lánc-SZINTŰ összecsukást használ a korábbi, kizárólag `standalone`-ban élt páciens-szintű „N terv” kapcsoló helyett — alapból csak a legfrissebb VÉGLEGESÍTETT verziójú lánc van nyitva (`domain/planChainData.ts` `rendezettLancok()`/`legfrissebbVeglegesVerzio()`), a láncok e szerint a dátum szerint csökkenően rendezve, nem a lánc indulási dátuma szerint. A lánc-fejléc (nyitottságtól függetlenül mindig renderel) a lánc LEGFRISSEBB verzióját mutatja (`domain/planFolders.ts` `legfrissebbVerzio()`, a korábbi `versions[0]`-bug javítása) + csukott állapotban a lánc végösszegét; a lánc legfrissebb verziósora 2+ verziós láncon „Legutóbbi” jelvényt kap. Az EGYETLEN globális, mentetlen piszkozat (D21), ha a megjelenített pácienshez tartozik (`components/useAktivDraft.ts` `useAktivDraft()`/`sajatDraft()`, a D48 `feloldPatientDir()`-jét birtokolja), a hozzá tartozó lánc fejlécén nem-kattintható jelzést kap, ÉS egy önálló blokkot a láncok fölött (kontextus, workflow-lépés, utolsó módosítás, végösszeg — tétel-/fázisszám és előleg nélkül, sor nélküli piszkozatnál összeg nélkül), aminek kattintása/„Folytatás” gombja a piszkozat-felülírás-őrt (`runOrConfirm`) megkerülve navigál. A `useListStateMemory` (D43) lánc-nyitottsággal bővült, az `OsszesTervSection` keresőszövege is erre állt át — a nyitottság és a kereső is visszaáll böngésző-„vissza” navigációnál | A lánc-fejléc korábban a lánc LEGRÉGEBBI verziójának dátumát mutatta (tényleges bug), a láncok storage-enumerációs (rendezetlen) sorrendben jelentek meg, és az aktív piszkozat sehol nem látszott ezen a listán — csak a Kezdőlapon, amikor a doki már egy másik akciót indítana, és a felülírás-őr csak akkor jelezte a konfliktust. A page-szintű "N terv" kapcsoló a lánc-szintű toggle mellett már redundáns volt (két egymás utáni kattintás ugyanahhoz a verzió-listához) |
| D52 | Egy meglévő pácienshez induló ÚJ terv-lánc (`app/src/state/planIndulas.ts` `ujTervForrasPaciensbol()`, mindhárom belépési pontja: Korábbi tervek páciensszintű „Új terv”, a páciens-részletoldal első-terv CTA-ja, a Kezdőlap „+ Új kezelési terv” → „Meglévő páciens keresése”) örökli a doki által látott legutóbb VÉGLEGESÍTETT terv-verzió `nyelv`/`penznem` értékét (`domain/blankPlan.ts` `createBlankPlan()` opcionális harmadik paramétere) — az összes lánc közül a legfrissebb `VEGLEGES` verziót nézve, nem csak az aktuális láncét. Véglegesített terv híján (vadonatúj páciens, vagy csak PISZKOZAT-státuszú tervekkel rendelkező páciens) a globális alapérték (`settings.alapertelmezettNyelv`/mindig `HUF`) marad érvényben. Az `orvos` mező ezen a paraméteren SOSEM örökölhető — az öröklés kizárólag erre a két mezőre szorítkozik; az orvosnak saját, önálló globális default-forrása van (D67) | Egy visszatérő páciensnek, akivel korábban németül/EUR-ban tárgyalt a doki, nem logikus, hogy minden új terv-lánc csendben visszaváltson magyar/HUF alapértékre — ez felesleges, elfelejthető átállítást rótt a dokira, aminek elmulasztása rossz nyelvű/pénznemű ajánlatot eredményezett. A `paciens`-pillanatkép forrásától (törzsadat vs. korábbi terv, D33) függetlenül ugyanaz a nyelv/pénznem-forrás, hogy a két belépési pont ne térjen el egymástól |
| D53 | „Új verzió” kizárólag a lánc LEGFRISSEBB verziósoráról indítható (`components/PatientPlanChains.tsx` — gombként csak a legfrissebb soron jelenik meg, historical soron egyáltalán nincs, se gombként, se menüpontként, D58) — pontosan a forrás verzió pillanatképéből, auto-frissítés nélkül (D7), és közvetlenül a Kezelések workflow-lépésre navigál. A betöltött piszkozat `statusz`-a a betöltés pillanatában PISZKOZAT-ra áll (`state/AppState.tsx` `loadPlanIntoDraft()`) — a `tervId` (a lánc-hovatartozás jele) érintetlen marad, a mentés így is ugyanabba a láncba kerül `v<n+1>`-ként | A historical sorról indított „Új verzió” korábban jelöletlenül a lánc fejére került, félrevezető sorrendet adva a Korábbi tervek listán — a régi verzióból induló módosítás helyes útja a „Másolás új tervbe” (D26), ami explicit új láncot hoz létre. A `statusz` korábban a forrás verzióé maradt (`VEGLEGES`), ami hamis „véglegesítve” jelvényt mutatott a szerkesztő fejlécén, és elmaradt a `PISZKOZAT-` letöltési előtag egy még el sem mentett piszkozatnál |
| D54 | A globális, több-pácienses terv-lista nem üzleti felület: a `Korábbi tervek` képernyő a DEMO oldal `Összes terv` fülére költözik (`/demo/tervek`), a `/tervek` URL redirectet kap. A terv-lánc/verzió fa (`components/PatientPlanChains.tsx`) elsődleges gazdája a páciens-részletoldal `Kezelési tervek` tabja (D35/D44), a DEMO áttekintő másodlagos. A DEMO öt füle URL-címezhető (`/demo/:tab`), a fülváltás `replace` navigáció | A redesign IA a `Korábbi tervek`-et a páciens tabjába olvasztotta, de a megvalósítás (D39) csak a belépési pontokat vette el — az oldal árván, elérhetetlenül, mégis teljes képernyőként maradt a kódban és a specben. A `replace` navigáció nélkül a `useListStateMemory` (D43/D51) POP-alapú megőrzése fülbe zárva halott lenne — a doki keresőszövege/lánc-nyitottsága elveszne minden fülváltásnál |
| D55 | Az „Új terv indítása" köztes páciensválasztón (D40) az új-páciens ág a kereső kártya FÖLÉ költözik, és rangot vált: a `PaciensekPage.tsx` „+ Új páciens" gombjával szó szerint azonos megjelenésű (solid, alapértelmezett méret) és feliratú, mindig látható, ELSŐDLEGES gomb — a korábbi, kereső alatti, másodlagos (`variant="soft" color="gray"`) „Vadonatúj páciens" stílus helyett. A két utat egy „vagy" feliratú vonalas elválasztó tagolja. A kártyán belüli, no-match „Új páciens: „…"" pszeudó-opció (a begépelt névvel előtöltve) változatlan marad. A feliratrendszer (`docs/03-funkcionalis-spec.md` § „Ebből következik a feliratok kötelező rendszere") alól ez a gomb kimondott kivételt kap: a „új terv" fogalmát ezen a képernyőn a fejléc („Új terv indítása") hordozza, nem az egyes akciógombok — ahogy a kereső találati sorai is felirat nélkül indítanak új tervláncot | A képernyőnek nem volt vizuális elsődleges akciója: a fő akció pontosan azt a stílust viselte, amit a projekt a „Mégse"/másodlagos akcióra tart fenn, miközben a fentről lefelé olvasási sorrend (keresés előbb, új páciens utóbb) fordítottja volt a doki mentális modelljének (előbb dönt új vs. visszatérő páciensről, utána keres). A `PaciensekPage.tsx` gombjával azonos megjelenés a felismerhetőséget szolgálja — cserébe azonos felirat mellett a két gomb kimenetele eltér (ott csak páciens jön létre, itt a terv-workflow is elindul), amit a képernyő fejléce old fel |
| D56 | Az „Új terv indítása" köztes páciensválasztó „Legutóbbi páciensek" listája (D40) 15 elemig mutat (`UJ_TERV_RECENT_LIMIT`, `domain/paciensAktivitas.ts`) — SZÁNDÉKOSAN eltérve a Kezdőlap D39 5-ös limitjétől (`RECENT_PACIENS_LIMIT`), ugyanazzal a `legutobbAktivPaciensek()` helperrel, csak más `limit`-paraméterrel hívva. A sor emellett EGYSOROS elrendezésre vált: a név balra, az aktivitás-szöveg (`aktivitasSzoveg()`) jobbra, ugyanazon a soron — ellentétben a Kezdőlap kétsoros `RecentRow`/`PatientListRow` mintájával (D47), amit ez NEM ír felül, mert más komponens | Az `/uj-terv` választó a doki tényleges elsődleges belépési pontja egy visszatérő pácienshez (a Kezdőlap „legutóbbi" kártyája csak tájékoztató villantás); egy szűkebb 5-ös lista itt túl gyakran kényszerítette gépelésre, holott a cél épp a gépelés elkerülése. 15 sor két sorban túl hosszúra nyúlt volna, ezért az egysorosítás a lépték-növelés előfeltétele volt, nem önálló ízlésbeli döntés |
| D57 | A „Másolás új tervbe" (`planMasolatKent()`, `app/src/domain/planCopy.ts`) a `paciens` blokkot az ÉLŐ törzsadatból (`paciens-adatok.json`, D33) veszi, ha a pácienshez van lezárt törzsadat — a forrás verzió `paciens` pillanatképe csak ennek hiányában marad érvényben. A `paciensId` ettől függetlenül változatlanul a FORRÁS tervből jön (D26): ez tartja a másolatot a forrás páciens-mappájában, a master saját `paciensId`-je nem veheti át ezt a szerepet. Olvashatatlan (sérült JSON vagy magasabb `schemaVersion`) törzsadatnál a másolás hibaüzenettel áll meg, nem esik vissza némán a forrás pillanatképére | D25 kettős elve („a szakmai tartalmat a KIJELÖLT verzióból, a páciensadatot az ÉLŐ masterből") a másolás bevezetésekor csak a páciensszintű „Új terv" úton (`ujTervForrasPaciensbol()`) teljesült — a „Másolás új tervbe" a forrás verzió esetleg hónapokkal korábbi páciensadatát (pl. elavult telefonszám) vitte tovább, holott a doki a másolás pillanatában a JELENLEGI adatot várja az új ajánlatba. Nem sérti a D33-at: új dokumentum előtöltéséről van szó (mint a `planUjTorzsadattal`-nál), nem kétirányú szinkronról — a forrás verzió pillanatképe (D7) és a törzsadat egyaránt érintetlen marad, a másolás csak OLVAS |
| D58 | A terv-lánc fa verziósora (`components/PatientPlanChains.tsx`) a lánc LEGFRISSEBB versióján két látható gombot mutat — elsődleges „Új verzió", másodlagos „Megnézés" (`soft`, gray) —, a `⋯` menü ott Letöltésre és Másolás új tervbére szűkül; egy historical (nem legfrissebb) soron nincs látható gomb, csak a `⋯`: Megnézés, Letöltés, elválasztó, Másolás új tervbe, **Ugrás a legfrissebb verzióra**. Az „Ugrás a legfrissebb verzióra" azonos oldalon belüli scroll+fókusz a lánc dobozára (`data-plan`), majd egy `requestAnimationFrame`-ben a lánc-fejléc toggle gombjára (a menü FocusScope-ja miatt egy szinkron fókuszváltás a triggerre esne vissza). Egy historical verzió másolásakor (van a láncnak nála frissebb verziója) egy figyelmeztető dialógus jelenik meg — a `runOrConfirm`/`AlertDialog` MEGLÉVŐ mechanizmusának bővítése, FÜGGETLENÜL attól, van-e mentetlen piszkozat; a pontos (exact) másolás a megerősítés után is lefut, a piros szín csak a piszkozat-vesztés ágon marad. A verziósor egésze (a gombokon/`⋯`-n kívül) továbbra sem kattintható | A `docs/03` korábbi „nincs látható akciógomb" indoklása (zsúfoltság sok egymás melletti hosszú felirattól) a D53 óta gyengült: láncon belül legfeljebb EGY sor kap „Új verzió"-t, tehát legfeljebb 2 látható gomb kerül egy sorra — pontosan a `docs/07-felulet-rendszer.md` „legfeljebb két látható gomb egy adatsoron" szabálya alá esik. A historical-másolás figyelmeztetés nélkül a doki észrevétlenül dolgozhatott egy elavult ajánlat-változaton, ha nem vette észre, hogy nem a legfrissebb sort nyitotta meg |
| D59 | A kezelésszerkesztő oldal alaplayoutja rögzítve: a fogtérkép-panel alapból csukva nyílik (a csukott gomb felirata puszta „Érintett fogak", darabszám nélkül), és a terv-szintű összegzés (Mindösszesen/Kerek végösszeg/Előleg/Tétel-leírások nyomtatása) kizárólag a fázislista végén, a „Fázis hozzáadása" gomb alatt jelenik meg. Emellett egy VADONATÚJ (`tervId === ''`) ÉS még sor nélküli piszkozaton az első fázis keresője a lap betöltésekor automatikus fókuszt kap — egy betöltött terven („Új verzió", „Másolás új tervbe") vagy már sort tartalmazó fázison ez a fókusz nem sül el (egy UTÓLAG hozzáadott fázis keresőjének fókuszát lásd D64) | A fogtérkép/összegzés-elhelyezés a kód szintjén már korábban is így működött, csak dokumentálva nem volt — egy jövőbeli olvasó tévesen nyitott kérdésnek gondolhatta volna. Az autofókusz a valódi hiányzó darab volt: a doki eddig mindig kattintott a keresőmezőbe, mielőtt gépelhetett volna, holott a `CLAUDE.md` „A UX kritikus pontja" szerint a gépel → nyíl → Enter ciklus egérhasználat nélkül kell működjön. A `piszkozatTartalmas()` (`domain/piszkozat.ts`) NEM alkalmas diszkriminátor: az a páciensnévre is igazat ad, így a normál Terv adatai → Kezelések útvonalon sosem sülne el |
| D60 | A kezelésszerkesztő fázisfejléce négy vezérlőt kapott: egymástól független nyitás/csukás (csukva a fejléc a nevet, a sorok számát és a fázisösszeget mutatja), ↑/↓ sorrendezés (a szélen a megfelelő irányú nyíl letiltva, `PriceListAdminPage.tsx` kategória-sorának `moveCategory()` mintáján, `app/src/domain/blankPlan.ts` `generaltFazisNev()`/`fazisNevGeneralt()` dönti el, hogy egy fázisnév a generált „N. kezelés" mintát követi-e — csak ez frissül mozgatáskor, kézzel átírt név nem), és a „Fázis törlése" szöveges gomb kuka-ikonra cserélve (az `aria-label` változatlan, a meglévő tesztek nem törnek). A fázisfejléc ↑ ↓ 🗑 hármasa NÉVESÍTETT KIVÉTEL a „legfeljebb két látható gomb egy adatsoron" szabály (`docs/07-felulet-rendszer.md`) alól — a fázisfejléc szekciófejléc, nem lista-jellegű adatsor, ugyanaz a besorolás, mint a kategória-admin során. A fázismegjegyzés mező a sor „+ leírás" progresszív elrejtés-mintáját kapta (alapból csukva, ha üres). Sor törlése egyetlen sorra változatlanul azonnali (nincs megerősítés), de a törölt sor helyén egy inline „Visszavonás" sáv jelenik meg rövid ideig, ami visszaállítja a sort az eredeti pozíciójába — a törölt `Sor` + index a `PhaseSection` helyi state-jében él, NEM egy általános undo-stack | A fázis-összecsukás/sorrendezés a redesign-interjú explicit kérése volt, ami korábban egyáltalán nem létezett a kódban (a `docs/03` „sorrendezhető" állítása addig drift volt, tényleges kód nélkül). A `Fazis`-nak nincs stabil azonosítója (`docs/02-domain-modell.md`) — a sorrendezés a meglévő `fazisResetToken` remount-mintát bővíti (a törléskor már bevált megoldás), az összecsukott/nyitott állapotot viszont egy, a szülőben élő, index-újraindexelt/felcserélt halmaz tartja meg a remount-ok között, hogy egy mozgatás ne csukja vissza az összes fázist. A sortörlés Undo-ja azért marad inline sáv, nem globális toast: a `docs/07` „nem toast, ha a hiba egy mezőhöz tartozik" szabályának szellemével összhangban a visszajelzés a törölt sor pontos pozíciójában marad, nem egy globális felugvón |
| D61 | A "Terv adatai" lap cím mezője (`TervCimField`) két írási útvonalon él: egy MÁR MENTETT lánchoz (`plan.tervId !== ''`) azonnal ír a MEGLÉVŐ `storage.savePlanLabel`-lel, ugyanúgy, ahogy a `PatientPlanChains.tsx` ceruza-ikonja; egy VADONATÚJ lánchoz a beírt érték a `DraftMeta.tervCim`-ben (a piszkozat UI-workflow metaadata, D37 mintája) él, és a `PreviewPage.tsx` `doFinalize()`-ja írja ki, KÖZVETLENÜL a `storage.savePlan()` után, saját try/catch-ben. A plan-mappa neve emiatt VÁLTOZATLANUL az élő `javasoltTervCim()`-ből képződik (D29) — a kézzel beírt cím sosem befolyásolja a fizikai mappanevet. Egy véglegesítés utáni címke-írási hiba NEM jelenti azt, hogy "a mentés nem sikerült" — a terv ekkor már a lemezen van, a hiba a siker-képernyőn egy külön, amber jelzésként jelenik meg | A `terv-cimke.json` mechanizmus (D29) `patientDir`+`planDir` azonosítót igényel, ami egy még sosem mentett lánchoz nem létezik — nincs értelme egy második, párhuzamos perzisztencia-utat építeni. Ha a címke-írási hiba a mentés közös hibazónájába esne, a doki újrapróbálná a véglegesítést, és egy fölösleges, D4-et sértő üres v2 verziómappa jönne létre |
| D62 | A "Terv adatai" lap "Dátumok" szekciójában az `ervenyesIg` (érvényesség vége) szerkeszthető, a `keltezes` (kiadás dátuma) marad automatikus, csak olvasható (D22 szerint a betöltés pillanatában bélyegzett). Az `ervenyesIg` alapértéke `plan.keltezes + settings.ervenyessegNap`, NEM `todayIso() + ervenyessegNap` — a visszaállítás a nyomtatványon megjelenő kiadás dátumához mért ablakot ad, nem a mai naphoz mérten. A mező soha nem maradhat üresen: kiürítve, a mező elhagyásakor automatikusan visszaáll az alapértékre. Egy kézzel átírt `ervenyesIg`-et a következő "Új verzió" (`frissDatummal`, D22) változatlanul némán felülír — ez szándékos, a kézi ablak nem öröklődik verziónyitáskor | D61 explicit kéri a szerkeszthetőséget: néha a doki egy konkrét ajánlatnál hosszabb/rövidebb érvényességet szeretne adni, mint a globális `settings.ervenyessegNap` alapérték. Az üresen-hagyás elleni védelem azért kényszerített (nem csak figyelmeztetés), mert egy üres `ervenyesIg` a `formatLongDate`-en át "Invalid Date"-ként kerülne egy szerződéses dokumentumra |
| D63 | A `nemetEngedelyezve` funkciókapcsoló megszűnt: a német nyelv mindig választható, gate nélkül — a Terv adatai lap nyelv/pénznem szekciója (a pénznem-szekció is, aminek korábban semmi köze nem volt a némethez), a Beállítások "Egyéb" tab alapnyelv-választója és német készültség-blokkja, valamint a Nyomtatványok tab HU/DE sablon-nyelvváltója mind feltétel nélkül látszanak. A nyelv/pénznem-kártya emellett a teljes piszkozat-életciklus alatt szerkeszthető marad (a korábbi `locked = plan.tervId !== ''` zárolás megszűnt) — csak a véglegesítés hozza létre az immutable pillanatképet. `formatMoney(value, currency, nyelv)`/`formatPrice(ar, currency, nyelv)` (`domain/money.ts`) új, kötelező `nyelv` paramétert kap: az ezres/tizedes elválasztó a nyelvtől függ (`hu` szóköz, `de` pont, a `domain/date.ts` `formatLongDate` mintáján), a tizedesjegyek száma és a pénznemjel változatlanul a pénznemtől | A `docs/06-arlista-import.md`-ben dokumentált, folyamatosan csökkenő lektorálatlan német tartalom nem indokolja, hogy a nyelv választása egy külön be-/kikapcsolható funkció mögé legyen zárva — a hiányos tartalom kezelése finalizációs figyelmeztetés kérdése (D21 HU-visszaesés + a Terv adatai lap előre jelzése), nem elrejtett funkcionalitás. Egy már létező német terv szerkeszthetősége korábban sem függhetett volna egy később kikapcsolt globális kapcsolótól — a korábbi `plan.nyelv === 'de'` escape-hatch pont ezt a problémát kerülgette workaround-dal; a flag megszűnésével ez az escape-hatch feleslegessé vált, és el is tűnt a kódból. A zárolás feloldása a 47. tétel (D52) nyelv/pénznem-öröklését teszi ténylegesen használhatóvá: az örökölt érték korábban is módosítható lett volna, csak épp a "Új verzió" útvonalon zárolva volt |
| D64 | A tételkereső billentyűzet-ciklusa (gépel → nyíl → Enter → a kereső kiürül és visszakapja a fókuszt) VÁLTOZATLAN marad tételhozzáadás után — a redesign-interjú fókuszt a Fog mezőre vivő, majd onnan Enterrel a keresőre visszaléptető javaslata (redesign D99/D100) EXPLICIT ELVETVE. Új fázis hozzáadásakor viszont az ÚJ fázis keresője automatikus fókuszt kap, és a lap odagördül — ez a `fokuszCel` mechanizmus (`PlanEditorPage.tsx`) bővítése egy `fazisKereso` ággal, a fogtérkép-kattintás meglévő mintáján | A gépel → nyíl → Enter ciklus a `CLAUDE.md` „A UX kritikus pontja" szerint az app legfontosabb, dokumentált és tesztelt versenyelőnye az Excellel szemben — egy ennyire központi viselkedés cseréje csak explicit user-jóváhagyással történhetett volna, ez nem történt meg. Az új fázis autofókusza ezzel szemben nem töri, hanem erősíti a ciklust: minden fázis a kereséssel indul, a doki fázisváltáskor sem kényszerül kattintásra |
| D65 | A kezelésszerkesztő során (`LineRow`) a "kézzel átírt" név-jelzés és reset NYELVFÜGGETLEN, tisztán `domain/nev.ts` `nevAtirt()`-re épül — ez a `sorFallback()`-tól (D21) FÜGGETLEN, más kérdésre válaszoló komparátor: `nevAtirt()` a felvételkor kapott (`resolveNev()` visszaesési) névhez mér, ezért egy DE tervnél, fordítás nélküli, egyébként érintetlen sort NEM jelez tévesen "átírt"-nak — a `nevKoveti()` (D24) ott mindig hamis lenne, mert az a "követi-e az árlistát ezen a nyelven" kérdésre válaszol, nem a "kézzel eltérítve van-e" kérdésre. A `sorFallback`-ot ezért TILOS úgy módosítani, hogy HU terven is fusson — a két komparátor egymás mellett él, külön célra. Az Ajánlati ár mező mellé felár-jelzés (`+X%`, amber) és mindhárom mezőn (név, ár, leírás) egy-egy kompakt reset-vezérlő került, ami a FELVÉTELKORI árlistai értéket állítja vissza — sosem az aktuális árlista frissítését (az a `arlista-snapshot` egy jövőbeli tétele). A leírás-reset a MEGLÉVŐ `leirasKoveti()`-t (D27) köti be a szerkesztőbe, a hozzá tartozó forrás-szöveget a kiemelt `arlistaiLeiras()` adja. A becsült ár (`≈`) widget pozíciója az Ajánlati ár mező ALÁ költözött, a widget maga (ghost `IconButton` + `≈` szövegglyph) változatlan | Régi vakfolt: a `sorFallback`-alapú "átírt" jelvény csak MELLÉKESEN fedte a kézi eltérítést DE terven, mert egy másik (fordítás-hiány) kérdésre válaszolt — magyar terven, ahol a `sorFallback` korán `null`-t ad, a kézi átírás jelöletlen maradt. Reset korábban egyik mezőn sem létezett, holott a `leirasKoveti()` domain-logika már régen készen állt hozzá, csak a szerkesztő sosem hívta |
| D66 | Az Előleg (`Plan.elolegOsszeg`) SZÁZALÉKRÓL ABSZOLÚT ÖSSZEGRE váltott — a doki tudatosan gépeli be a pénznem alapegységében. A `0–100%`-os szorítás (ami korábban strukturálisan garantálta `előleg ≤ fizetendő`-t) emiatt megszűnt: ha a fizetendő egy utólagos sortörlés/módosítás miatt az előleg alá csökken, az előleg értéke VÁLTOZATLAN MARAD (nem vágódik le automatikusan), és ez KEMÉNY véglegesítési blokk, amíg a doki nem rendezi (`domain/veglegesitesOr.ts` `elolegTullep`, `domain/totals.ts` `elolegTullepi`). Egyenlőségnél (`előleg === fizetendő`) a fennmaradó rész explicit `0`, ez legitim. Bekapcsoláskor a mező üresen, azonnali fókusszal jelenik meg, előtöltés nélkül; a kötelező-mező hiba csak blur/véglegesítési kísérlet után látszik; egy explicit `0` beírása blur/Enter után automatikusan kikapcsolja a kapcsolót. Egy régi `terv.json` `elolegSzazalek` mezője betöltéskor figyelmen kívül marad, nincs migráció | A redesign-döntéssorozat (backlog-64/DP-047) explicit ezt kérte, annak ellenére, hogy a százalék-alapú tárolás tudatos, drift-mentes tervezési döntés volt (a régi indoklás: "nem lehet elcsúszni a sorok és a belőlük számolt előleg között"). A doki a kérdés feltevésekor tudatosan vállalta ezt a kockázatot egy fix összeg javára — a validációs lánc csak az `előleg ≤ fizetendő` HATÁRT kényszeríti ki, a "még mindig ésszerű arány"-t nem. Az automatikus levágás (clamp) explicit elvetett alternatíva volt: a doki tudatos rendezését várja, néma korrekció helyett |
| D67 | A kezelőorvos additív `Settings` mezőkkel bővült: `inaktivOrvosok?: string[]` (a jelenleg deaktivált nevek, hiányuk = aktív) és `alapertelmezettOrvos?: string` (hiányzó/inaktív érték esetén az első aktív név a tényleges default) — `schemaVersion` nem emelkedett. A feloldás egyetlen helye `app/src/domain/orvosok.ts`. Öröklési szabály: egy ÚJ terv-lánc mindig a globális default orvossal indul, a páciens korábbi tervétől függetlenül; egy „Új verzió” a forrás verzió orvosát örökli, HA az még aktív, egyébként a globális defaultra esik vissza, semleges info-sávval (a `PlanEditorPage.tsx` meglévő dátum-info sávjának bővítése, nem külön csatorna); a „Másolás új tervbe” mindig a globális default orvossal indul, a forrás orvosa SOSEM másolódik át. A Kezelőorvos szekció a „Terv adatai” lapon (Radix `Select`, csak az aktív orvosok közül) NEM esik a D4 `tervId`-alapú zárolás alá — az kizárólag a nyelvre/pénznemre vonatkozik. Egy orvosnév TÖRÖLHETŐ (nem csak deaktiválható) — explicit eltérés a D17 mintától, mert a `plan.orvos` NÉV-pillanatkép, nem `id`-hivatkozás, tehát egy törölt/deaktivált orvosra hivatkozó korábbi terv olvashatósága nem múlik az `id` megőrzésén | A `Settings.orvosok` korábban sima névlista volt, aktív/inaktív jelölés és alapértelmezett-orvos fogalom nélkül — a doki sehol nem tudott egy konkrét tervhez másik orvost választani, ha 2+ név szerepelt a listában. A D17-mintájú „csak deaktiválható” szabály itt feleslegesen korlátozó lenne egy kis, egy-két orvosos rendelőben, ahol egy elgépelt/elavult név eltávolítása ne igényeljen „deaktiválás örökre” döntést |
| D68 | A véglegesítés harmadik KEMÉNY blokkja: a terv `orvos` mezője üres vagy nem szerepel a jelenleg AKTÍV orvosok között (`domain/veglegesitesOr.ts` `veglegesitesDiagnozis()` `orvosProblema` mezője), a MEGLÉVŐ `nameMissing`/`uresSorok` mintáján — az `alkalmazhato` PUHA lánc-mapon KÍVÜL, ugyanúgy, mint a `masterElteresek`, hogy egy ott felvett mező ne váltson ki automatikusan megerősítő dialógust. A blokk sorrendje `nameMissing` UTÁN, `uresSorok` ELŐTT (mindkettő egy kattintással javítható a Terv adatai lapon, a tartalmi szerkesztést igénylő `uresSorok` marad utolsó) | Az aláírás-blokkban szereplő kezelőorvos-név jogilag releváns; egy üres vagy már nem aktív névvel kiadott PDF jogilag hibás dokumentum lenne. Egy már véglegesített terv `plan.orvos` név-pillanatképét ez visszamenőleg nem érinti (D7) |

## Adatvédelmi keret

A kezelési terv tartalma — név, születési idő, lakcím, TAJ, elvégzendő
beavatkozások — **GDPR 9. cikk szerinti különleges adat**.

A D2 és D3 döntés következménye, hogy az adat soha nem hagyja el a rendelő
gépét és a doktor saját Google fiókját. A fejlesztő semmilyen minőségben
nem kerül az adatkezelési láncba.

Amit a rendelőnek kell rendeznie (nem fejlesztési feladat, de szólni kell
róla):

- **Google Workspace kell, nem ingyenes Gmail-fiók.** Workspace-nél van
  Data Processing Amendment; ingyenes fióknál nincs, és a mappanevekben
  páciensnevek lesznek.
- A gyökérmappa **ne** a `Letöltések` legyen — az sok gépen automatikusan
  személyes OneDrive-ra szinkronizál.
- Lemeztitkosítás (BitLocker) legyen bekapcsolva.
- A Drive szinkron **nem backup**: negyedévente kézi másolat külső lemezre.

## Kockázatok

| Kockázat | Kezelés |
|---|---|
| A német tartalom (118 név + jogi szöveg) sosem készül el | Az MVP magyarul teljes értékű. A kapcsoló nincs elrejtve (D21): a doki bekapcsolhatja és kipróbálhatja a német módot, a hiányzó fordítások száma számszerűen látszik a Beállításokban és a Terv adatai lapon, a hiányzó nevű tételek magyarra esnek vissza jelöléssel, a véglegesítés pedig figyelmeztet, mielőtt a páciens aláírna egy részben magyar nyelvű dokumentumot |
| A doki lassabbnak találja az Excelnél | A tételfelvitel billentyűzetes flow-ja az első, amit tesztelni kell — a PDF előtt |
| A Drive „streamelés" módban lassú `paciensek/` fa olvasás | A Drive kliensben **Tükrözés** módot kell beállítani, nem Streamelést |
| A `Fog` mező jegyzetmezővé válik | Elfogadott: szabadszöveget is elbír, csak az esetleges későbbi automatika nem indul el |
| Windows 260 karakteres útvonalkorlát | Rövid mappanevek, tiltott karakterek szűrése — lásd `02-domain-modell.md` |

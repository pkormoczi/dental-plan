# arlista-nap
Type: doki
Prio: later
Source: adattisztítás, a doki közös ülésén gyűjthető

Fél nap, egy ülésben, nincs hozzá kód:
- `gyakori` csillagozás (ma mind a 118 tétel `false`) — 8–12 tétel lesz a szerkesztő gyorsgombja
- a két `SAVOS` tétel (`t014`, `t016`) alsó határa: az Excelben `"35-55000"` alakban állt, most a
  felső határhoz igazítva (`35 000`/`38 000`), jóváhagyásra vár
- kétes tételek: `t072`/`t073` azonos 95 000 Ft (az egyik felesleges?), `t078` „Sín” besorolása
  (`k10` → `k12`?), `t064`/`t066` azonos 10 000 Ft a kvadránsos eltérés mellett,
  `t054`/`t055`/`t056` implantátum-névforma
- `k04`/`k05`/`k06`/`k12` fogtérkép-szín (ma alap szürke), a kategória-panelben átszínezhető
- garancia-szakasz magyar szövege (ma placeholder, a Beállítások → Nyomtatvány szövegei alatt);
  utána nézni, marad-e csaknem üres PDF-oldal
- tétel-leírások és `csomag` jelölések begyűjtése (ma egyik tételen sincs)
